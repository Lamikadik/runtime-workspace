# runtime-workspace
Runtime workspace for local experiments with memory dumps and serialized cache formats. Contains heap snapshots, stack traces, and encoded data segments from test environments. Used for debugging session restoration and analyzing compressed runtime states. Not intended for production or external use.
